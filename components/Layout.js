export default function Layout({ children }) {
  return (
    <div className="font-poppins transition ease-in-out overflow-x-hidden bg-light2 text-gray-800 dark:bg-dark2">
      {children}
    </div>
  );
}
